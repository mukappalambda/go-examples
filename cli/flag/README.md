# Flag Demo

```bash
Usage of ./flag:
  -bs int
        batch size (default 32)
  -criterion string
        criterion (default "mse")
  -dropout float
        dropout probability (default 0.2)
  -epochs int
        epochs (default 10)
  -optimizer string
        optimizer (default "adam")
```
