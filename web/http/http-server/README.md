# HTTP Server Example

Build:

```bash
make build
```

Start the server:

```bash
./bin/http-server
```

Send HTTP requests:

```bash
curl http://localhost:8080/data
```

Clean:

```bash
make clean
```
