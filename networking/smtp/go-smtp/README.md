# go-smtp

```console
$ go build
$ ls ./go-smtp
./go-smtp
$ ./go-smtp -h
Usage of ./go-smtp:
  -username string
        username
$ touch .password
$ PASSWORD_PATH=.password ./go-smtp -username "<your-email-account>"
$ go clean
```
