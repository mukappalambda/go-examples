# kvstore

Build the binaries:

```bash
make build
```

Start the server:

```bash
./bin/kvstore-server
```

Run the CLI:

```bash
./bin/kvstore-cli -key foo -value bar
./bin/kvstore-cli -key foo
```

Clean:

```bash
make clean
```
